import TopBarAuth from "../components/TopBarAuth";
import "./styles/Auth.scss";
const Auth = () => {
  return (
    <main className="Auth">
      <TopBarAuth />
    </main>
  );
};

export default Auth;
